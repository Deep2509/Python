# Write a Python program to calculate the length of a string.

str1=input("Enter string: ")
count=0

for i in str1:
    count+=1 #We can increase the counter by one with every iteration

print("Count is: ",count)

